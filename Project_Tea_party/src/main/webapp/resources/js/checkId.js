
/*
const checkId = document.getElementById("checkId");

function checkId(event){
    event.preventDefault();
    const inputId = checkId.value;
    const param = {"inputId" : inputId }

    $.ajax({
        url : "/checkId",
        type : 'post',
        data : param,
        success : function(data) {
            alert(`${data.inputId}는 사용가능한 아이디 입니다.`)
        },
        error : function() {
            alert("error");
        }
    });

}
checkId.addEventListener("click", checkId)
 */
